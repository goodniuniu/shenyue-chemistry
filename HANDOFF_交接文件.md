# HANDOFF 交接文件 · 申悦学习-AI指导化学学习

> 生成时间：2026-09-19（建库首日全量交付）。本文档供切换模型/新会话快速接手本项目。
> 读完本文件 + `README.md` + `知识库/00_知识库总索引.md` 即可继续工作。
> 蓝本：物理项目（`E:\MyOutput\AI_Project\申悦学习-AI指导物理学习`）；建站依据：根目录《多学科复习辅助项目_建站指导手册.md》。
>
> **接手第一步（新会话按序做）**：①读本文件与 `知识库/复盘追踪/知识掌握状态表.md`；②确认周复盘定时任务存活（**automation_f46fdeb2-bdf4-461a-91f3-2d50916c57c0**，每周日 **21:17** Asia/Shanghai，错开数学 20:37、英语 20:57、物理 20:17；2026-09-19 晚重建并试跑成功，原 automation-176e0a29 已废弃）；③问用户三件事的最新进展：学校进度（定机动线）、月考时间、GitHub 账号；④若用户发来照片/文件，直接按第 6 节工作流处理。

---

## 1. 项目背景与目标

- **服务对象**：家长（很久没接触化学）+ 女儿**申悦**（2026 年 9 月升高二）。
- **近期目标**：国庆假期 **2026.10.1（四）–10.7（三）共 7 天**化学专项，备战**高二 10 月化学月考**（具体时间待确认）。
- **长期目标**：建立亲子化学学习交流方式（家长=提问的记者，孩子=讲课的小老师），陪伴到 **2028 年高考**。
- **项目目录名**已更正为 `申悦学习-AI指导化学学习`（2026-09-19；此前误写为「申悦学校」，正文中的「申悦学校」均为"申悦的学校"的合法表述，不在改名范围内）。

## 2. 当前状态（2026-09-19 交付）

| 事项 | 状态 |
|------|------|
| 骨架复制（物理蓝本：build 脚本/KaTeX/vite/方法维度/卡片模板） | ✅ |
| 三校真题入库（2025.10 华附/广大附中/执信 原卷+解析 PDF） | ✅ 归档 `知识库/化学/素材与拓展/真题试卷/`（含提取文本，**只供检索考点，做题用原卷**） |
| 真题分析报告 + 主线确定 | ✅ 第一主线 = 选必一《热效应》+《速率与平衡》（三校最大公约数）；机动线三选一（电化学/水溶液/必修，默认电化学） |
| 知识库建库 | ✅ 化学 9 项（概念 4 + 题型 2 + 陷阱 1 + 过关 30 问 + 方程式总表）+ 方法 2 卡（物理复制）+ 状态表 + 艾宾浩斯日历（国庆起）+ 索引 |
| 国庆 7 天计划 v1 | ✅ `国庆7天辅导计划_10月1-7日.md`（含摸底分流表、机动线三选一、成功标准 5 条） |
| 家长支持四件套 | ✅ 速成指南（反应热与平衡篇）+ 提问卡 30 问 + 错题入库速查 + 长假专项框架 |
| 静态网站 | ✅ `python build_site.py` → `docs/` 25 页，全部 HTTP 200 实测；内部链接 0 断链（含根级页面→kb 路径修复，蓝本遗留问题本项目已修） |
| 打印包 | ✅ `打印包/国庆打印包_化学.pdf`（12 页 A4 五部分：30 问 + 方程式默写留空版+答案 + 陷阱 10 条 + 守恒速查 + 提问卡 30 问）+ 预览拼图；构建时同步 `docs/print/`，首页有下载入口 |
| 周复盘定时任务 | ✅ 每周日 **21:17** Asia/Shanghai（**automation_f46fdeb2-bdf4-461a-91f3-2d50916c57c0**，2026-09-19 晚重建并试跑成功：第 1 周复盘维持基线、更新状态表并提交 7d34f45）。复盘后 build + commit（远程未配则跳过 push） |
| 终验数据（2026-09-19） | ✅ 网站 25 页全 HTTP 200、内部断链 0、公式页 28 个 KaTeX 公式渲染无 `$` 泄漏（浏览器实测）、打印包 PDF 下载 200；打印包 12 页逐页目检通过 |
| git | ✅ 远程已配置并推送（git@github.com:goodniuniu/shenyue-chemistry.git，2026-09-19 晚首次推送）；工作区干净 |
| GitHub Pages 部署 | ⏳ 未做——用户给账号后按 `部署说明_GitHubPages.md`（建议仓库名 `shenyue-chemistry`） |

## 3. 关键假设（待确认，影响后续）

1. **学校进度未确认** → 国庆 Day4 机动线默认电化学线（执信卷 60% 考点）。10.1 摸底当晚由家长问一句确认；说不清则走默认。
2. **月考时间未确认** → 暂假设国庆后（10 月中下旬）。若在 9 月底，国庆计划整体转为"考后补弱"。
3. **教材版本按人教版**（选必一《化学反应原理》）假设；广东新高考选考科目。
4. **选科组合未确认**（化学为选考之一）。
5. 三校真题为 **2025.10**（上一届高二）卷，作为本届月考范围的最佳可得证据。

## 4. 目录结构与文件职责

```
国庆7天辅导计划_10月1-7日.md             # 源：国庆计划 v1（改完重新 build）
家长支持/                                # 源：速成指南/提问卡/错题速查/长假框架
知识库/00_知识库总索引.md / 化学/索引_化学.md / 方法/索引_方法.md
知识库/化学/{核心知识网络,典型题型与方法,易错警示与辨析,概念检验,素材与拓展}/
知识库/化学/素材与拓展/真题试卷/         # 3 校原卷+解析 PDF；提取文本/ 子目录
知识库/化学/素材与拓展/方程式总表.md     # 化学第二生命线（滚动默写，只增不减）
知识库/复盘追踪/{知识掌握状态表,艾宾浩斯复习日历_国庆起}.md
build_site.py                           # Markdown → docs/（同蓝本；单页清单/首页已化学化）
build_print_pack.py                     # 知识卡 → 国庆打印包_化学.pdf（已加 Windows 字体回退）
build_preview.py                        # 打印包 PDF → 打印包/预览_contact-sheet.png（PDF 更新后重跑）
pdf_extract.py                          # 真题 PDF → 提取文本（pymupdf）
打印包/ 国庆打印包_化学.pdf + 预览_contact-sheet.png
docs/                                   # 生成的网站（GitHub Pages 发布此目录，含 .nojekyll）
node_modules -> 物理项目node_modules     # 目录联接（junction），复用 vite，勿提交（.gitignore 已排除）
```

**维护流程**：改 Markdown 源 → `python build_site.py`（打印包相关再跑 `python build_print_pack.py`）→ git commit（配好远程后 push）。

## 5. 技术要点与坑（新模型必读，蓝本坑全部继承 + 本项目新增）

1. **npm/node**：本机 node 在 `C:\Users\admin\AppData\Local\nvs\default\`（在 PATH）；Kimi 内置运行时路径见蓝本 HANDOFF。本项目 **node_modules 是指向物理项目的目录联接**（`mklink /J`），vite 直接可用，无需 npm install；换机器需重建联接或安装 vite。
2. **vite GBK 修复**（`vite.config.js`，勿删）：Windows 客户端把中文 URL 转 GBK 再编码，vite 抛 "URI malformed"。
3. **kb 页面相对路径 = `"../" * len(rel.parts)`；`kb/index.html` 用 `"../"`**（蓝本踩过，本项目沿用）。
4. **卡片标题禁 ASCII 双引号**（用「」）；`build_site.py` 有 html.escape 兜底。
5. **markdown-it-py 必须 `.disable("linkify")`**；文件读写一律 `encoding="utf-8"`；Python 控制台避免 print 中文/emoji 之外还要注意 `sys.stdout.reconfigure(encoding="utf-8")`。
6. **【本项目新增】根级单页里链接知识库要用 `kb/...` 站点路径**，写 `知识库/...` 仓库路径会 404（蓝本两代项目均有此隐藏断链，本项目已全量修复；新增根级页面时注意）。
7. **【本项目新增】reportlab 字体回退**：`build_print_pack.py` 优先读 `DAIMON_CJK_FONT_*` 环境变量（Kimi 环境用），缺失时回退 `C:\Windows\Fonts\msyh.ttc / msyhbd.ttc`（`subfontIndex=0`）。**msyh 缺 ⇌（U+21CC）和 Unicode 上下标字形** → 脚本已处理：⇌ 用 Segoe UI Symbol（seguisym.ttf）注册 SymCJK 兜底并 `<font>` 包裹；上下标字符转 `<sub>/<super>` 标签。
8. **【本项目新增】section() 截断坑**：解析 30 问卡时结束标记必须整行匹配 `\n---\n`，写 `---` 会撞上表格分隔行 `|----|` 把答案表截成光表头（已修，曾致 B 表 21–30 题丢失）。
9. PDF 提取文本公式符号大量丢失——只可用于检索考点，做题用原卷 PDF（本项目的提取文本在 `真题试卷/提取文本/`）。
10. **杀进程要杀树**：`netstat -ano | grep ":<port>"` 找 PID 后 `taskkill //F //T //PID <pid>`；**绝不留服务器运行**。
11. **打印包逐页目检**：PDF 渲染 PNG 后目检（judge 通道图片注入故障时，用 Read→CDN URL→`mcp__4_5v_mcp__analyze_image` 链路，本日已验证可用）。
12. **【本项目新增】vite 会把依赖缓存写进 `docs/.vite/`**（dev server 以 docs 为根目录运行时）。已加入 `.gitignore`；若误提交，`git rm -r --cached docs/.vite` 后重提交。
13. KaTeX 已本地化（`assets/katex/` woff2），模板用 `__BASE__` 占位符不能 `.format()`；验收公式渲染用浏览器 DOM 计数（`document.querySelectorAll('.katex').length` 应 >0 且正文无残留 `$...$`），比截图更可靠。

## 6. 常用工作流（用户会直接下的指令）

- "入库化学错题/试卷"（附照片或文件）：提取考点 → 按模板建卡 → **涉及方程式同步登记方程式总表打 ⚠️** → 更新索引与状态表 → `python build_site.py`。
- "查化学+关键词"：检索知识库卡片并回答。
- "本周化学知识库复盘"：五步复盘（过状态表 → 逐卡自测 → 更新状态 → 下周计划 → build + commit/push）。
- "按长假专项框架设计寒假复习计划"：读最近考试分析 + 框架 → 输出完整日程。
- 新增任何内容后：`python build_site.py` 同步网站。

## 7. 待办与下一步（按优先级）

> 当前进度参照：**专项未开始**（今天 2026-09-19 建库交付，假期 10.1 才启动；9.22–9.30 为在校周，每周日 21:17 自动周复盘已恢复——9.19 晚已试跑第 1 周复盘，其间若无用户输入基本无动作）。

1. **10.1 摸底当晚**：确认学校进度 → 锁定机动线（电化学/水溶液/必修）；若水溶液线触发，对 AI 说"补建水溶液线"（广大附中卷 5/10/11/13/14/16 为题源）。
2. **月考时间确认**：若在 9 月底，国庆计划转"考后补弱"模式（Day6 换本校月考卷复盘）。
3. 假期中：错题照片随时"入库化学错题"；Day6 模拟后批量入库。
4. 考后：拍卷"入库化学错题" → 失分分析 → 校准寒假专项主线。
5. GitHub Pages：用户给账号后按 `部署说明_GitHubPages.md` 发布（仓库建议名 `shenyue-chemistry`；SSH 走 443 + 代理 7897 的网络方案沿用物理项目）。
6. 长期：方程式总表随进度滚动扩充（水溶液组、电极反应式组）；陷阱卡随错题入库追加；每轮周复盘更新状态表与"本周复盘摘要"。

## 8. 沟通基调备忘

- 家长化学零基础：类比先行（记账/扶梯/弹簧/保险箱/双向扶梯），避免公式轰炸。
- 关系先于成绩，夸行为不夸聪明；假期每天学习 ≤ 4 小时，晚上只留 15 分钟回讲，**假期首先是假期**。
- 化学特色话术：方程式默写是每天仪式（早晚各 10 分钟）；错题必关联方程式。
- 所有面向用户的回复用中文；文件命名 `[时间标签]_[科目]_[维度]_[知识点].md`。
